Shared base Prettier config used for my projects.

# Usage

```shell
npm install --save-dev @sergei-dyshel/prettier-config
```

Add to `package.json`:

```json
"prettier": "@sergei-dyshel/prettier-config"
```
